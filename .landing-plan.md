# LOU-128 + LOU-129 Landing — Pre-flight Checklist

## Worktree
- Path: `/Users/iansmith/louis14/.claude/worktrees/agent-a4146648cb26b0d58`
- Branch: `worktree-agent-a4146648cb26b0d58`
- Master HEAD (base): `6bfbd7d8ea7acc6733740e77ce8f0f93f8dfab0b`
- LOU-129 HEAD: `b52569939bb757c588cf1198b981ac4ce4261eae`

## Fonts symlink
- `fonts -> /Users/iansmith/louis14/fonts` (in place)

## Pre-merge symbol verification (Conflict 2)
Checked LOU-129 branch for callers of the legacy marker symbols (the
ones referenced in the conflicting `createMarkerPseudoElement` hunks):

| Symbol | Defined in | External callers (outside createMarkerPseudoElement)? |
|---|---|---|
| `ListMarker` (type, used as `ListMarker{}`) | `pkg/layout/list_marker.go` | None — only `ListMarker{}.MarkerTextWithSuffix(...)` in layout_tree_builder.go |
| `MarkerTextWithSuffix` | `pkg/layout/list_marker.go` | None outside layout_tree_builder.go createMarker code |
| `GetListStyleCategory` | `pkg/layout/list_marker.go` | None outside layout_tree_builder.go createMarker code |
| `markerTextSourceLegacy` | `pkg/layout/layout_tree_builder.go` (the file itself) | None — defined and used in the same conflicting region |
| `InlineMarginsForInside` | `pkg/layout/list_marker.go` | None outside layout_tree_builder.go createMarker code |
| `applyMarkerInlineMargins` | `pkg/layout/layout_tree_builder.go` | None — defined and used in the same conflicting region |

Note: `ListMarker` STRING appears in many comments and similar names
(e.g. `UnpositionedListMarker`, `generateListMarker` in louis13/),
but the actual `ListMarker{}` struct type is only used by the
conflicting createMarker code path on the LOU-129 branch.

**Verdict: dead code on LOU-129 side.** Safe to discard the LOU-129
marker hunks (take HEAD for hunks 2 + 3 of layout_tree_builder.go).
The `list_marker.go` file will remain after the merge as orphan
defined-but-unused exported symbols — that's a known consequence of
HEAD's css-lists Phase 1 rewrite superseding LOU-129's marker work.
Not a design escalation; matches the brief's prescribed outcome.

## Planned resolutions
- `pkg/css/style.go`: concatenate (HEAD first, LOU-129 second).
- `pkg/layout/layout_tree_builder.go`: take HEAD for all 3 hunks.
