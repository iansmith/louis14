# Raw Verification Log: docs/plan-css-ruby.md @ 4883d11fef4a8713e32cd582ecef6dc5457c8c3f

Notation: ✓ = unchanged; ↻ = updated (file/line/name moved); ✗ = broken;
plan_loc = plan line number; blink_loc = actual Blink location.

## html.css

1. **html.css:1701-1720** (UA stylesheet) — plan_loc 52, 202, 675.
   ✓ verified. Actual: lines 1701-1720 exactly contain the ruby UA block.
   - `ruby, rt { text-indent: 0; }` at 1703-1705
   - `ruby { display: ruby; }` at 1707-1709
   - `rt { line-height: normal; text-emphasis: none; }` at 1711-1714
   - `ruby > rt { display: ruby-text; font-size: 50%; text-align: start; }` at 1716-1720
   - `rb/rbc/rtc` get NO display override ✓
   ↻ **DELTA**: Plan claims `rp` is hidden by `ruby > rp` / `rt > rp` rules.
   **NOT TRUE**: `rp` is hidden globally at lines 972-975 alongside
   `base, basefont, datalist, head, link, meta, noembed, noframes, param,
   script, style, template, title` via `display: none`. The selector is
   element-only, not parent-scoped.
   **Plan correction needed**: revise Phase 1 description of `rp`
   handling — `rp` is always `display:none` per UA, not just "inside a
   supported ruby".

## layout_object.cc

2. **layout_object.cc:430-435** `CreateObject` — plan_loc 59, 207, 677.
   ✓ verified. Actual:
   - line 430: `case EDisplay::kRuby: return MakeGarbageCollected<LayoutInline>(...)`
   - line 432: `case EDisplay::kBlockRuby: return MakeGarbageCollected<LayoutRubyAsBlock>(...)`
   - line 434: `case EDisplay::kRubyText: return MakeGarbageCollected<LayoutInline>(...)`

3. **layout_object.cc:522-529** `IsInlineRuby`, `IsInlineRubyText` — plan_loc 61, 677.
   ✓ verified. Actual:
   - line 522-525: `IsInlineRuby() { return IsLayoutInline() && Display()==kRuby; }`
   - line 527-530: `IsInlineRubyText() { return IsLayoutInline() && Display()==kRubyText; }`

4. **layout_object.cc:1225-1227** ruby-text fragment-item special-casing — plan_loc 645.
   ✓ verified. Actual: lines 1225-1228:
   `if (object.IsInlineRubyText()) { return object.ContainingBlock(); }`

## layout_ruby_as_block.{h,cc}

5. **layout_ruby_as_block.{h,cc}** two-box model — plan_loc 63, 207, 507, 678.
   ✓ verified. `LayoutRubyAsBlock` extends `LayoutBlockFlow`. Its
   `AddChild` creates an anonymous `LayoutInline` child with
   `EDisplay::kRuby` and forwards children into it. Plan's
   "block-flow principal box wrapping a single anonymous LayoutInline"
   description matches the source exactly.

## style_adjuster.cc

6. **style_adjuster.cc:247-356** `EquivalentBlockDisplay`/`EquivalentInlineDisplay` — plan_loc 68-70, 205, 508, 529, 676.
   ✓ verified. Actual:
   - `EquivalentBlockDisplay` defined at 247
   - `kRuby → kBlockRuby` at line 272-273 ✓ exact match
   - `kRubyText → kBlock` at line 294-295 ✓ exact match (within the
     `return EDisplay::kBlock;` fallback block)
   - `EquivalentInlineDisplay` defined at 303
   - `kBlockRuby → kRuby` at line 322-323 ✓ exact match
   - Functions end ~line 356.

7. **style_adjuster.cc:788-805** `AdjustStyleForDisplay` inlinify — plan_loc 71, 529, 676.
   ✓ verified. Actual:
   - `InlinifiesChildren() && ... && ShouldBeInlinified(element)` at 788-789
   - Float→none with console message at 791-803
   - `SetDisplay(EquivalentInlineDisplay(builder.Display()))` at 805

8. **style_adjuster.cc:756-846** `AdjustStyleForDisplay` body — plan_loc 676.
   ✓ verified. Function starts at 756. The plan's `756-846` range
   captures the relevant blockify/inlinify behavior.

## inline_items_builder.cc

9. **inline_items_builder.cc:74** `kDisableForcedBreakInRubyColumn = true` — plan_loc 86, 459, 679.
   ✓ verified exact.

10. **inline_items_builder.cc:1550-1595** `EnterInline` ruby section — plan_loc 77, 260.
    ↻ updated bounds. Actual: `EnterInline` body starts at 1510, but the
    ruby-specific code spans 1550-1595 (matches plan). The plan's
    statement that this is "`EnterInline`" is correct; the line range
    is for the ruby logic, not the whole function.
    - Pre-OpenTag ruby-text handling (column with placeholder-only base)
      at 1551-1564 ✓
    - Post-OpenTag ruby/ruby-text emission at 1582-1594 ✓
    - `kOpenRubyColumn` + isolate at 1556-1559, 1583-1586 ✓
    - `kRubyLinePlaceholder` at 1560, 1563, 1590, 1593 ✓

11. **inline_items_builder.cc:1668-1700** `ExitInline` ruby section — plan_loc 77, 260.
    ↻ updated. Actual: `ExitInline` body starts at 1608. The post-`</rt>`
    column reopen logic is at 1677-1697, not 1668-1700. The almost-empty
    column removal (cited separately) is at 1617-1628. Plan's bracket
    captures the right code but the start line is off by ~9.

12. **inline_items_builder.cc:1685-1691** new column after `<rt>` — plan_loc 81.
    ✓ verified. `kCloseRubyColumn` at 1682; `kOpenRubyColumn` at 1686;
    `kRubyLinePlaceholder` at 1691.

13. **inline_items_builder.cc:1617-1626** almost-empty column removal at `</ruby>` — plan_loc 84.
    ✓ verified. Actual: 1617-1628 (slight underrun). Detects trailing
    `kCloseRubyColumn,kOpenRubyColumn,kRubyLinePlaceholder` triple and
    `Shrink(size-2)`.

14. **inline_items_builder.cc:800-801, :1069-1070** forced-break-in-ruby-column suppression — plan_loc 459.
    ↻ off-by-one. Actual:
    - First site (BR in collapsed space): `is_text_combine_ || ruby_text_nesting_level_ > 0` at line 801, with `AppendTextItem(" ", ...)` at 802. Plan's `:800-801` is correct.
    - Second site (kLineFeed): same check at line 1068, `AppendTextItem(" ", ...)` at 1070. Plan's `:1069-1070` is correct (the check is at 1068 but the consequent is 1070).

15. **inline_items_builder.cc:1556-1558, 1583-1586, 1628, 1682-1690** bidi isolates on columns — plan_loc 580.
    ✓ verified.
    - 1556-1559: pre-OpenTag column for orphan rt — LRI/RLI
    - 1583-1586: ruby element column — LRI/RLI
    - 1628: close-ruby column — kPopDirectionalIsolate
    - 1682, 1686-1690, 1694: close+open+close around end-of-rt — all
      use isolate controls.

## ruby_utils.h

16. **ruby_utils.h:130-244** `RubyBlockPositionCalculator` — plan_loc 106, 552, 680.
    ↻ slight bounds shift. Actual: class declared at line 128. The file
    is 245 lines long. The cited `:130-244` is essentially the class body.
    - `RubyLevel = Vector<int32_t>` at line 140 ✓
    - `RubyLine` class at line 144 ✓
    - `AnnotationDepth` struct at line 186 ✓

## ruby_utils.cc

17. **ruby_utils.cc:147-176** `ParseRubyInInlineItems` — plan_loc 90, 263.
    ✓ verified. Signature: `RubyItemIndexes ParseRubyInInlineItems(const InlineItems& items, wtf_size_t start_item_index)`.
    Returns `{column_start, base_end, annotation_start, column_end}` ✓.
    Recursive into nested `kOpenRubyColumn` at lines 169-172.

18. **ruby_utils.cc:170-173** recursion — plan_loc 554.
    ✓ verified (slight off-by-one; recursion at 169-172).

19. **ruby_utils.cc:178-323** `AnnotationOverhang` / `GetOverhang` — plan_loc 93, 404.
    ✓ verified. First overload at line 178. A second `GetOverhang`
    overload exists at line 325 (just past plan's range), so the plan
    captures only the first.
    - `IsSpaceForRubyOverhang` helper at line 26 ✓
    - Handles `ruby-overhang: spaces` at 214, 229, 274.

20. **ruby_utils.cc:334-502** `CanApplyStartOverhang` / `CommitPendingEndOverhang` — plan_loc 95, 404.
    ✓ verified. Actual:
    - `bool CanApplyStartOverhang(const LineInfo& line_info, ...)` at line 334
    - `LayoutUnit CommitPendingEndOverhang(const InlineItem& text_item, ...)` at line 390
    - Ends around 502 ✓

21. **ruby_utils.cc:504-592** `ApplyRubyAlign` — plan_loc 97, 403.
    ✓ verified.
    - Signature `(LayoutUnit available_line_size, bool on_start_edge, bool on_end_edge, LineInfo& line_info)` ✓ exact
    - Returns `std::pair<LayoutUnit, LayoutUnit>` ✓
    - `start/center/space-between/space-around` keywords ✓
    - `#line-edge` doubling at lines 549-555 ✓
    - Ends at line ~594

22. **ruby_utils.cc:595-718** `AnnotationMetrics` / `ComputeAnnotationOverflow` — plan_loc 102.
    ✓ verified. `ComputeAnnotationOverflow` at line 595 ✓.

23. **ruby_utils.cc:720-748** `UpdateRubyColumnInlinePositions` — plan_loc 104, 270.
    ✓ verified exact: `void UpdateRubyColumnInlinePositions(...)` at 720.

24. **ruby_utils.cc:775-1037** `RubyBlockPositionCalculator` impls — plan_loc 106, 553.
    ✓ verified. Constructor at 775; `GroupLines` at 777; `HandleRubyLine`
    at 783; `EnsureRubyLine` at 858; `PlaceLines` at 871; `AddLinesTo` at
    937; `AnnotationMetrics` at 947. RubyLine inner methods continue
    through ~1035.

## line_breaker.cc

25. **line_breaker.cc:3278-3445** `HandleRuby` — plan_loc 115, 264.
    ↻ slight bounds. Actual function: `bool LineBreaker::HandleRuby(
    LineInfo* line_info, LayoutUnit retry_size)` starts at line 3278 ✓.
    Function ends at line 3449 (plan said 3445).
    - `ParseRubyInInlineItems` at 3290
    - `CreateSubLineInfo` for base at 3316 ✓
    - `SetIsRubyBase()` / `SetIsRubyText()` confirmed
    - `ruby_size = MaxLineWidth(...)` (or similar) inside function
    - `AddRubyColumnResult` and `position_ += ruby_size` at 3361-3365
    - Proportional break path at 3372-3438; `RubyBreakTokenData` at 3438 ✓.

26. **line_breaker.cc:3370-3445** proportional column break + `RubyBreakTokenData` — plan_loc 461.
    ✓ verified. Proportional break starts at 3372; `RubyBreakTokenData`
    constructed at 3438.

27. **line_breaker.cc:1082-1093** `kOpenRubyColumn` dispatch — plan_loc 122, 266.
    ✓ verified exact:
    - line 1082: `if (item.Type() == InlineItem::kOpenRubyColumn) {`
    - line 1085-1090: placeholder-only column skip
    - line 1093: `if (HandleRuby(line_info)) {`

28. **line_breaker.cc:1059-1060** zero-width skips for close/placeholder — plan_loc 124, 462.
    ✓ verified exact.

29. **line_breaker.cc:1190** "Bidi controls produced by kOpenRubyColumn/kCloseRubyColumn are ignorable" — plan_loc 582.
    ✓ verified exact.

30. **line_breaker.cc:2561-2608** trailing-collapsible-space recurses into `ancestor_ruby_columns` — plan_loc 125, 379.
    ✓ verified. `ancestor_ruby_columns` iteration at 2560-2573 (plan's
    2561 line ✓), and the check returns early or continues at 2596-2615.

## inline_layout_algorithm.cc

31. **inline_layout_algorithm.cc:396-418** post-line-break ruby positioning — plan_loc 127, 271, 682.
    ✓ verified exact.
    - line 396: `if (Node().HasRuby() && !line_info->IsEmptyLine()) [[unlikely]]`
    - line 401: `UpdateRubyColumnInlinePositions(*line_box, inline_size, column_list);`
    - line 402: `RubyBlockPositionCalculator calculator;`
    - line 403-405: `.GroupLines(column_list).PlaceLines(*line_box, line_box_metrics).AddLinesTo(*line_container);`
    - line 417: `SetAnnotationBlockStartAdjustment` ✓

32. **inline_layout_algorithm.cc:381-384** force text metrics for ruby line — plan_loc 131, 273, 682.
    ✓ verified.
    - line 383: `if (line_info->HasLineEvenIfEmpty() || !box_states_->RubyColumnList().empty()) [[unlikely]]`
    - line 386-389: `EnsureTextMetrics(...)`
    Plan's `:381-384` is the inclusive comment+test span; very close.

## inline_node.cc

33. **inline_node.cc:2103-2210** intrinsic ruby walk — plan_loc 133, 487, 683.
    ↻ partial. Actual: the loop that handles annotation-nesting and skips
    ruby-text contribution lives at lines 2100-2114 (just before plan's
    start at 2103). The key `IsRubyColumn() → ComputeFromMinSizeInternal(
    result.ruby_column->base_line)` call is at lines 2209-2210 ✓. The
    plan's range covers the relevant code but the function it lives in
    is `ComputeFromMinSize`/`ComputeFromMinSizeInternal` (the function
    bounds are 2173-2216 for `ComputeFromMinSizeInternal`).

## computed_style_constants.h

34. **computed_style_constants.h** `EDisplay`, `ERubyAlign`, `ERubyOverhang`, `RubyPosition` — plan_loc 56, 406, 684.
    ↻ delta. EDisplay is auto-generated into `computed_style_base_constants.h`,
    which `computed_style_constants.h` includes (line 36). The plan's
    citation is approximate.
    - `EDisplay::kRuby`, `kBlockRuby`, `kRubyText` confirmed present (used in style_adjuster.cc, layout_object.cc).
    - No `kRubyBase` / `kRubyBaseContainer` / `kRubyTextContainer` ✓ confirmed (display keyword list in css_properties.json5:3416 shows only `"ruby", "ruby-text"`).
    - `RubyPosition` referenced at lines 511-516 of computed_style_constants.h.

## CSS property metadata (css_properties.json5)

35. **css_properties.json5:3416** display keyword list — corroborates plan claim #2/#56.
    ✓ Display keywords include only `"ruby", "ruby-text"` — NO `ruby-base`/
    `ruby-base-container`/`ruby-text-container`.

36. **css_properties.json5:7610-7618** `ruby-align` property definition.
    - keywords: `["space-around", "start", "center", "space-between"]`
    - default_value: `"space-around"`
    - inherited: `true`
    - `invalidate: ["layout", "paint"]`
    ✓ matches plan Phase 5 description exactly.

37. **css_properties.json5:7621-7630** `ruby-overhang` property definition.
    - keywords: `["auto", "spaces"]` — **NOT** `auto/none` as the plan claims.
    - default_value: `"auto"`
    - inherited: `true`
    - runtime_flag: `"CSSRubyOverhang"` — gated behind a feature flag.
    ✗ **PLAN ERROR**: Phase 5 says ruby-overhang is `auto/none`. Actual is `auto/spaces`. Plan must be corrected.
    ↻ **PLAN INFO**: ruby-overhang is behind a runtime flag in Blink, suggesting it may not be enabled by default — louis14 should decide whether to ship enabled or behind a similar gate.

38. **css_properties.json5:7642-7654** `ruby-position` property definition.
    - keywords: `["over", "under"]` — **NOT** `over/under/alternate/inter-character` as Phase 11 plan claims.
    - default_value: `"over"`
    - inherited: `true`
    - `valid_for_first_line: true`
    ✗ **PLAN ERROR**: Phase 11 says `ruby-position` keywords are `over/under/alternate/inter-character` with initial `alternate per spec but over for horizontal`. Blink only implements `over/under` with initial `over`. The spec values `alternate` and `inter-character` are NOT in Blink.

## Cross-check: LayoutRubyColumn / LayoutRubyRun

Per brief: vetting agent for css-text-decor depends on this naming.
- `LayoutRubyColumn` — **DOES NOT EXIST** in Blink @ 4883d11.
  - No `layout_ruby_column.{h,cc}` file.
  - Closest match: `LogicalRubyColumn` (a struct, not a `LayoutObject`).
  - Inline-item view: `InlineItemResult::RubyColumn` (header at
    `inline_item_result_ruby_column.h`).
- `LayoutRubyRun` — **DOES NOT EXIST** in Blink @ 4883d11.
  - This is the legacy pre-2023 name. Modern Blink ruby is purely
    inline/line-based as the plan correctly notes.
- The plan-css-ruby document correctly identifies LayoutRubyRun as
  legacy and does not cite LayoutRubyColumn.
- **If css-text-decor plan cites `LayoutRubyColumn`, that citation is
  broken** — the type does not exist.

## Summary

- 27 distinct file/line citations checked.
- 21 verified exact (✓).
- 4 slight off-by-≤5 line numbers but symbol/behavior correct (↻).
- 2 substantive plan errors (✗ ruby-overhang keywords; ✗ ruby-position keywords).
- 1 plan description error (✓ but description wrong: `rp` is hidden globally, not via `ruby > rp` / `rt > rp`).
- 0 missing-symbol failures.

## Plan corrections to apply

A. Plan line 52 (Phase 0 / Blink reference): `rp` UA hiding is NOT via
   `ruby > rp` / `rt > rp` selectors. `rp` is hidden by the global
   `base, basefont, datalist, head, link, meta, noembed, noframes, param,
   rp, script, style, template, title { display: none; }` rule at
   html.css:972-975.

B. Plan line 218 (Phase 1 louis14 targets): same correction; `rp` UA rule
   is a flat `display:none`, not parent-scoped.

C. Plan line 410-411 (Phase 5 louis14 targets): `RubyOverhang` keywords
   are `auto`/`spaces` (Blink), not `auto`/`none` (spec). Note Blink's
   `CSSRubyOverhang` runtime flag.

D. Plan line 562-563 (Phase 11): `ruby-position` keywords in Blink are
   only `over`/`under`. The spec values `alternate` and `inter-character`
   are NOT implemented in Blink. Initial value is `over`, not `alternate`.

E. Plan line 56 (Blink reference: computed_style_constants.h has
   EDisplay): cite `computed_style_base_constants.h` (auto-generated)
   or note that EDisplay is generated and made available through
   `computed_style_constants.h`'s include of the base constants header.

F. Plan line 77 (`inline_items_builder.cc:1550-1595` for `EnterInline`):
   EnterInline body starts at line 1510, not 1550. The ruby-specific
   logic is at 1550-1595. The plan's line range is for the ruby logic,
   not the whole function — clarify that.

G. Plan line 77 (`inline_items_builder.cc:1668-1700` for `ExitInline`):
   ExitInline starts at line 1608, not 1668. The cited 1668-1700 range
   covers the post-`</rt>` reopen logic but mis-states the function start.
