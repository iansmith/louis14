# Raw verification log — plan-css-text-decor.md
SHA: 4883d11fef4a8713e32cd582ecef6dc5457c8c3f

## applied_text_decoration.h (56 lines total)
- Class `AppliedTextDecoration`: lines **18-48** (plan says 20-56 → off by ~5)
- `AppliedTextDecorationVector`: line **50** (plan says 52 → off by 2)
- Field `lines_`: line **43** (plan says 45 → off by 2)
- Field `style_`: line **44** (plan says 46 → off by 2)
- Field `color_`: line **45** (plan says 47 → off by 2)
- Field `thickness_`: line **46** (plan says 48 → off by 2)
- Field `underline_offset_`: line **47** (plan says 49 → off by 2)
- Behavior: ✓ unchanged. Five-field class, vector typedef present.

## text_decoration_info.cc (730 lines total)
- `ComputeLineData`: lines **549-604** (not previously cited but contains double/wavy geometry)
- `OffsetFromDecoratingBox`: lines **583-596** (plan says 344-352 → off by ~240; behavior unchanged: decorating_box_paint_offset - local_origin_.line_over)
- `ComputeUnderlineLineData`: lines **607-625** (plan says 354-370 → off by ~250)
- `ComputeOverlineLineData`: lines **627-646** (plan says 373-390 → off by ~250; uses `ComputeUnderlineOffsetForUnder` with TextTop position — plan's "TextTop position, flipped-vertical case" description is roughly correct)
- `ComputeLineThroughLineData`: lines **648-661** (plan says 393-400 → off by ~250; formula `2*FloatAscent/3 - thickness/2` matches plan's description exactly)
- `ComputeThickness`: lines **681-706** (plan says 417-432 → off by ~260; plan's description is partially correct but MISSES the spelling/grammar-error branch which now does platform-specific values, not auto/from-font/length only)
- `double_offset_from_thickness`: line **551** is `decoration.resolved_thickness + 1.0f` — plan's "double gap = thickness+1" is ✓
- Wavy uses same `double_offset_from_thickness` value — ✓
- Line-through uses `floorf(double_offset_from_thickness)` — plan's "floored for line-through" ✓

## text_decoration_info.h (280 lines total)
- Constructor: lines **169-176** (plan cites 169-176) ✓ exact match
- `BaselineForInkSkip()`: line **202** (plan says 225-227 → off by ~23)
- `ComputeUnderlineLineData/OverlineLineData/LineThroughLineData`: declared around 178-195 area
- `OffsetFromDecoratingBox`: line **213**
- `ComputeThickness`: line **214**
- **CRITICAL**: `ComputeUnderlineOffset` does NOT exist as a method on TextDecorationInfo. It exists as a method on `TextDecorationOffset` (a separate helper type), called from line 619 inside `ComputeUnderlineLineData`. Plan's pairing "`ComputeUnderlineLineData()` / `ComputeUnderlineOffset()`" should clarify the latter is on TextDecorationOffset.

## text_painter.cc (1391 lines total)
- `SetEmphasisMark`: lines **1066-1091** (plan says 539-567 → off by ~530, HUGE drift)
- `DrawEmphasisMarks` call: lines **563-566** (plan says 567-571 → close, off by ~4)
- Behavior: ✓ over offset is `-Ascent() - EmphasisMarkDescent(mark)`, under offset is `Descent() + EmphasisMarkAscent(mark)`. Plan description matches.
- New finding: there is now a ruby-aware branch (`text_item->HasOverAnnotation()/HasUnderAnnotation()`) under `RuntimeEnabledFeatures::TextEmphasisWithRubyEnabled()`, which adjusts the offset by `text_item->AnnotationMetrics().ascent/descent.Ceil()`. Plan mentions HasOverAnnotation/HasUnderAnnotation but doesn't note the feature flag.
- Plan's claim that paint order is "shadow → decorations-except-line-through → text → line-through → emphasis" — DrawEmphasisMarks call is at line 563-566 which is AFTER text/line-through painting per the surrounding code.

## inline_paint_context.h (161 lines total)
- `DecoratingBoxList` (type alias): line **20**
- `DecoratingBox` (class itself): in separate header `core/paint/decorating_box.h`
- `PushDecoratingBox` (template): lines ~**26-30** (one of several push variants)
- `PushDecoratingBoxAncestors`: also present
- `PushDecoratingBoxes`: present
- `PopDecoratingBox`: line **31**
- `SyncDecoratingBox`: line **121-123**
- All symbols exist ✓ Plan reference is correct, just no exact line numbers given (which is fine).

## text_decoration_painter.h (156 lines total)
- `class TextDecorationPainter`: line **64** (plan says 64-81 for Phase enum + methods, which is incorrect — that range is the constructor declaration)
- `enum Phase { kOriginating, kSelection }`: line **86** (plan cited 64-81)
- `Begin(...)`: line **87**
- `PaintExceptLineThrough(...)`: line **88**
- `PaintOnlyLineThrough()`: line **89**
- Plan's cited range 64-81 is WRONG — actual location is 86-89.

## Ruby: LayoutRubyColumn DOES NOT EXIST at this SHA
- `core/layout/inline/layout_ruby_column.h` returns 404
- Actual ruby layout headers at this SHA:
  - `core/layout/layout_ruby_as_block.{cc,h}` — block-level ruby box
  - `core/layout/inline/ruby_utils.{h,cc}` — `ParseRubyInInlineItems`, `GetOverhang`, `ApplyRubyAlign`, `RubyBlockPositionCalculator`, `ComputeAnnotationOverflow`, types `RubyItemIndexes`, `AnnotationOverhang`, `AnnotationMetrics`
  - `core/layout/inline/inline_item_result_ruby_column.h` — struct `InlineItemResultRubyColumn` (53 lines) with `base_line: LineInfo`, `annotation_line_list: HeapVector<LineInfo, 1>`, `position_list: Vector<RubyPosition, 1>`, `is_continuation`, `end_overhang`, `last_base_glyph_spacing`
- **DESIGN IMPACT**: Plan Phase 5 says "This mirrors Blink's `LayoutRubyColumn` / ruby line-box-expansion machinery". `LayoutRubyColumn` as a layout-object class does not exist — the correct reference is the **inline ruby-column model**: `InlineItemResultRubyColumn` (data) + `ruby_utils.h` machinery (`RubyBlockPositionCalculator`, `ComputeAnnotationOverflow`, `ApplyRubyAlign`) + `LayoutRubyAsBlock` (block wrapper). The plan's approach (column = base + annotation) is correct in spirit — Blink uses `InlineItemResultRubyColumn` for exactly that pairing — but the cited type name needs replacement.
