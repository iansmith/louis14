# css-will-change plan — Blink citation checklist

Pinned SHA: 4883d11fef4a8713e32cd582ecef6dc5457c8c3f

## Citations to verify

### File-path citations (with line ranges)

1. **Line 187** — `third_party/blink/renderer/core/style/style_will_change_data.h`
   - Symbols: `StyleWillChangeData`
   - Fields claimed: `values` (`Vector<AtomicString>`), `resolved_longhand_ids` (`CSSBitset`),
     `has_scroll_position_value`, `has_transform_property`, `has_any_transform_property`
   - Comment: "The bitset only contains resolved longhand CSSPropertyID(s). No aliases." (cited at L163-164)

2. **Line 197** — `core/style/computed_style.h`, ~lines 2550-2595
   - Symbols: `HasWillChangeProperty(CSSPropertyID id)`, `HasWillChangeTransformHint()`,
     `HasWillChangeHintForAnyTransformProperty()` (a.k.a. `HasWillChangeAnyTransformProperty`),
     `HasWillChangeOpacityHint`, `HasWillChangeFilterHint`, `HasWillChangeBackdropFilterHint`,
     `HasWillChangeScrollPositionHint`

3. **Line 208** — `third_party/blink/renderer/core/layout/layout_object.cc`, ~lines 1520-1580
   - Symbols: `ComputeIsFixedContainer(const ComputedStyle* style)`,
     `ComputeIsAbsoluteContainer(bool is_fixed_container, const ComputedStyle* style)`
   - Behavior claims:
     a. `HasTransformRelatedProperty()` triggers fixed container
     b. `GetPosition() != EPosition::kStatic || HasWillChangeProperty(CSSPropertyID::kPosition)`
        - **explicitly claimed asymmetric**: kPosition will-change ⇒ abspos only, NOT fixed
     c. `TransformStyle3D() == ETransformStyle3D::kPreserve3d`
     d. `ShouldApplyPaintContainment(style) || ShouldApplyLayoutContainment(style)`
        consulting `HasWillChangeProperty(CSSPropertyID::kContain)`
     e. `HasFilterInducingProperty()` / `HasBackdropFilter()`
     f. `HasNonInitialOffsetPath()`
     g. Root exception: early-returns for `LayoutView` / document element

4. **Line 211** — `computed_style.h` ~2740
   - Symbol: `HasTransformRelatedProperty()`
   - Behavior: `HasWillChangeTransformProperty() || has_transform_value || has_perspective_value`

5. **Line 233** — `computed_style.cc`
   - Symbols: `CanContainAbsolutePositionObjects()`, `CanContainFixedPositionObjects()`
   - Behavior: delegate to `ComputeIsAbsoluteContainer` / `ComputeIsFixedContainer`

6. **Line 236** — `third_party/blink/renderer/core/css/resolver/style_adjuster.cc`,
   `AdjustComputedStyle` (~lines 1240-1430)
   - Symbols: `ForcesStackingContext`, `AllowsZIndex`
   - Behavior: document element / top layer / view-transitions force SC; AllowsZIndex gate

7. **Line 247** — `PaintLayerStackingNode`
   - Behavior: z-sorts children of any `IsStackingContext()` layer
   - No explicit file cited — verify file path exists.

8. **Line 281-284** (Phase 1 reference) — repeats citations 1 & 2.

9. **Line 315-316** (Phase 2 reference) — repeats citation 6.

10. **Line 341-342** (Phase 3 reference) — repeats citation 3.

11. **Line 373-374** (Phase 4 reference) — repeats citation 3.

12. **Line 392-393** (Phase 5 reference)
    - Symbol: `LayoutObject::StyleDidChange`
    - Behavior: invalidates layout when CB-affecting properties change; will-change in that set

### Type / function citations (inline narrative, no file)

13. **Line 37** — `StyleWillChangeData` (covered by 1)
14. **Line 38** — `LayoutObject::ComputeIsFixedContainer`, `ComputeIsAbsoluteContainer` (covered by 3)
15. **Line 39** — `StyleAdjuster` (covered by 6)
16. **Line 119-122** — `StyleWillChangeData`, `HasWillChangeProperty(CSSPropertyID)` (covered)
17. **Line 144** — `LayoutObject::ComputeIsFixedContainer`, "root early-return for LayoutView" (covered)
18. **Line 163** — `StyleWillChangeData::resolved_longhand_ids` + the no-aliases comment (covered)
19. **Line 209-228** — full enumerated behavior of `ComputeIsFixedContainer` (covered by 3)

### CSS spec citations (not Blink)

- Line 23: `https://drafts.csswg.org/css-will-change-1/#will-change` — spec, not Blink, skip.
- Line 240: same spec link — skip.

## Audit pass — completeness

Inline file-name extraction (`.cc`/`.h`):
- style_will_change_data.h (line 187)
- computed_style.h (lines 197, 211)
- layout_object.cc (lines 208, 341)
- computed_style.cc (line 233)
- style_adjuster.cc (lines 236, 315)
- layout_object.cc reference at line 341 = same as 208

Symbol-only extraction (Blink type names without a file):
- StyleWillChangeData (37, 119, 163, 186, 281)
- ComputedStyle::HasWillChangeProperty (37, 121, 199, 218, 228, 316)
- LayoutObject::ComputeIsFixedContainer (38, 144, 207, 341, 373)
- LayoutObject::ComputeIsAbsoluteContainer (38, 207, 226, 341)
- StyleAdjuster::AdjustComputedStyle (39, 236, 315)
- PaintLayerStackingNode (247)
- LayoutObject::StyleDidChange (392)
- HasTransformRelatedProperty (210-211)
- HasWillChangeTransformProperty (211)
- HasFilterInducingProperty / HasBackdropFilter (220)
- HasNonInitialOffsetPath (221)
- LayoutView (144, 222, 342)
- EPosition::kStatic (213, 227)
- ETransformStyle3D::kPreserve3d (216)
- CSSPropertyID::kPosition (213, 228)
- CSSPropertyID::kContain (218)
- ShouldApplyPaintContainment / ShouldApplyLayoutContainment (217)
- CanContainAbsolutePositionObjects / CanContainFixedPositionObjects (232-233)
- ForcesStackingContext, AllowsZIndex (237-238)
- IsStackingContext (248)

All Blink-symbol mentions resolve to one of the 12 numbered citation groups above.

## Verification strategy

Fetch the following Blink files via gitiles raw at SHA 4883d11fef4a8713e32cd582ecef6dc5457c8c3f:

- `core/style/style_will_change_data.h`
- `core/style/computed_style.h` (look for HasWillChange* accessors and HasTransformRelatedProperty)
- `core/style/computed_style.cc` (CanContain* methods)
- `core/layout/layout_object.cc` (ComputeIsFixedContainer, ComputeIsAbsoluteContainer, StyleDidChange)
- `core/css/resolver/style_adjuster.cc` (AdjustComputedStyle, ForcesStackingContext, AllowsZIndex)
- `core/paint/paint_layer_stacking_node.h` / `.cc` (existence + z-sort behavior)

For each, capture exact line numbers and confirm the behavior description.
