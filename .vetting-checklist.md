# Citation Checklist: docs/plan-css-ruby.md vs Blink @ 4883d11fef4a8713e32cd582ecef6dc5457c8c3f

Format: `[#] <plan line>: <file:line> (<symbol>) — <claim>`

## Phase 0 / Blink reference model section

1. plan:52 — `core/html/resources/html.css:1701-1720` — UA: `ruby { display: ruby }`, `ruby > rt { display: ruby-text; font-size: 50%; text-align: start }`. `rb/rbc/rtc` get NO display. `rp` hidden by `ruby > rp` / `rt > rp`.
2. plan:56 — `computed_style_constants.h` — `EDisplay` has only `kRuby`, `kBlockRuby`, `kRubyText`. NO `kRubyBase`/`kRubyBaseContainer`/`kRubyTextContainer`.
3. plan:59 — `layout_object.cc:430-435` — `CreateObject`: `kRuby` → `LayoutInline`, `kRubyText` → `LayoutInline`, `kBlockRuby` → `LayoutRubyAsBlock`.
4. plan:61 — `layout_object.cc:522-529` — `IsInlineRuby()` = `IsLayoutInline() && Display()==kRuby`; `IsInlineRubyText()` = same w/ `kRubyText`.
5. plan:63 — `layout_ruby_as_block.{h,cc}` — block-level `<ruby>` (`display: block ruby`) generates `LayoutBlockFlow` principal wrapping single anonymous `LayoutInline` with `display: ruby`.
6. plan:68 — `style_adjuster.cc` `EquivalentBlockDisplay:272-273` — `kRuby` → `kBlockRuby` when blockified.
7. plan:69 — `style_adjuster.cc:294` — `kRubyText` → `kBlock`.
8. plan:70 — `style_adjuster.cc EquivalentInlineDisplay:322-323` — `kBlockRuby` → `kRuby`.
9. plan:71 — `style_adjuster.cc AdjustStyleForDisplay:788-805` — inlinification; `InlinifiesChildren()`; non-OOF child has `SetDisplay(EquivalentInlineDisplay(...))`; floats dropped to `float:none`.
10. plan:77 — `inline_items_builder.cc:1550-1595` `EnterInline` / `:1668-1700` `ExitInline`. On `IsInlineRuby()`: emit `kOpenRubyColumn` + `kRubyLinePlaceholder`.
11. plan:81 — `inline_items_builder.cc:1685-1691` — new `kOpenRubyColumn...kCloseRubyColumn` pair after each `<rt>`'s close.
12. plan:84 — `inline_items_builder.cc:1617-1626` — almost-empty columns removed at `</ruby>`.
13. plan:86 — `inline_items_builder.cc:74` `kDisableForcedBreakInRubyColumn` — forced breaks inside column become spaces.
14. plan:90 — `ruby_utils.cc:147-176` `ParseRubyInInlineItems` — given `kOpenRubyColumn` index, returns `{column_start, base_end, annotation_start, column_end}`; recurses.
15. plan:93 — `ruby_utils.cc:178-323` — `AnnotationOverhang {start,end}` + `GetOverhang(...)`. Honors `ruby-overhang`, `ruby-align`, collapsible spaces.
16. plan:95 — `ruby_utils.cc:334-502` `CanApplyStartOverhang` / `CommitPendingEndOverhang` — clamp overhang.
17. plan:97 — `ruby_utils.cc:504-592` `ApplyRubyAlign(available_line_size, on_start_edge, on_end_edge, line_info)` — returns insets; `start`/`center`/`space-between`/`space-around`; `#line-edge` doubling.
18. plan:102 — `ruby_utils.cc:595-718` `AnnotationMetrics` + `ComputeAnnotationOverflow(...)`.
19. plan:104 — `ruby_utils.cc:720-748` `UpdateRubyColumnInlinePositions(...)`.
20. plan:106 — `ruby_utils.h:130-244` `RubyBlockPositionCalculator`; `ruby_utils.cc:775-1037`. `RubyLevel` = `Vector<int32_t>`; `GroupLines`, `PlaceLines`, `AddLinesTo`, `AnnotationMetrics`.
21. plan:115 — `line_breaker.cc:3278-3445` `HandleRuby(line_info, retry_size)` — on `kOpenRubyColumn`: parse, build base + annotation sub-LineInfos, `ruby_size = MaxLineWidth(...)`, emit `RubyColumn` `InlineItemResult`. If doesn't fit: proportional break + `RubyBreakTokenData`.
22. plan:122 — `line_breaker.cc:1082-1093` — `kOpenRubyColumn` dispatch.
23. plan:124 — `line_breaker.cc:1059-1060` — `kCloseRubyColumn`/`kRubyLinePlaceholder` zero-width skip.
24. plan:125 — `line_breaker.cc:2561-2608` — trailing-collapsible-space removal reaches inside `ancestor_ruby_columns`.
25. plan:127 — `inline_layout_algorithm.cc:396-418` — `UpdateRubyColumnInlinePositions`, then `RubyBlockPositionCalculator.GroupLines().PlaceLines().AddLinesTo()`, `SetAnnotationBlockStartAdjustment`.
26. plan:131 — `inline_layout_algorithm.cc:381-384` — ruby line forced to have text metrics even if "empty".
27. plan:133 — `inline_node.cc:2103-2210` — intrinsic walks `kOpenRubyColumn` uses `result.ruby_column->base_line`; ruby intrinsic = `max(base, annotation)`.

## Phase 1 Blink references

28. plan:202 — html.css:1701-1720 (dup of #1)
29. plan:205 — `style_adjuster.cc:247-356` — `EquivalentBlockDisplay/EquivalentInlineDisplay`.
30. plan:207 — `layout_object.cc:430-435` (dup of #3); `layout_ruby_as_block.cc` — block-ruby two-box model.

## Phase 2 Blink references

31. plan:260 — `inline_items_builder.cc:1550-1700` — emit column items, fresh column after `<rt>`, remove trailing.
32. plan:263 — `ruby_utils.cc:147-176` `ParseRubyInInlineItems` (dup of #14).
33. plan:264 — `line_breaker.cc:3278-3445` `HandleRuby` (dup of #21).
34. plan:266 — `line_breaker.cc:1082-1093` (dup of #22).
35. plan:267 — `ruby_utils.{h,cc}` `RubyBlockPositionCalculator` (dup of #20).
36. plan:270 — `ruby_utils.cc:720-748` `UpdateRubyColumnInlinePositions` (dup of #19).
37. plan:271 — `inline_layout_algorithm.cc:381-418` (dup of #25/#26).

## Phase 4 Blink references

38. plan:379 — `line_breaker.cc:2561-2608` — trailing collapsible space recursion into ancestor_ruby_columns (dup of #24).

## Phase 5 Blink references

39. plan:403 — `ruby_utils.cc:504-592` `ApplyRubyAlign` — full algorithm incl. #line-edge doubling (dup of #17).
40. plan:404 — `ruby_utils.cc:178-323` `GetOverhang` (dup of #15); `:334-502` `CanApplyStartOverhang`/`CommitPendingEndOverhang` (dup of #16).
41. plan:406 — `computed_style_constants.h` `ERubyAlign` / `ERubyOverhang` enums.

## Phase 7 Blink references

42. plan:459 — `inline_items_builder.cc:74` `kDisableForcedBreakInRubyColumn` (dup of #13); `:800-801`, `:1069-1070` — forced breaks → spaces.
43. plan:461 — `line_breaker.cc:3370-3445` — proportional column breaking + `RubyBreakTokenData`.
44. plan:462 — `line_breaker.cc:1059-1060`, `:1190` — column bidi controls/placeholders ignorable break-wise.

## Phase 8 Blink references

45. plan:487 — `inline_node.cc:2103-2210` — intrinsic walk uses `result.ruby_column->base_line` (dup of #27); `ComputeFromMinSizeInternal`.

## Phase 9 Blink references

46. plan:507 — `layout_ruby_as_block.{h,cc}` two-box model (dup of #5).
47. plan:508 — `style_adjuster.cc:272-273,322-323` — `ruby`↔`block ruby` (dup of #6/#8).

## Phase 10 Blink references

48. plan:529 — `style_adjuster.cc AdjustStyleForDisplay:788-805` (dup of #9) + `EquivalentInlineDisplay:303-356`.

## Phase 11 Blink references

49. plan:552 — `ruby_utils.h:130-244` `RubyBlockPositionCalculator` full `RubyLevel`/`RubyLine` machinery (dup of #20); `ruby_utils.cc:775-1037`; `ParseRubyInInlineItems` recursion `:170-173`.

## Phase 12 Blink references

50. plan:580 — `inline_items_builder.cc:1556-1558,1583-1586,1628,1682-1690` — each ruby column wrapped in isolate (LRI/RLI...PDI).
51. plan:582 — `line_breaker.cc:1190` — controls ignorable.

## Phase 14 Blink references

52. plan:645 — `layout_object.cc:1225-1227` — ruby-text fragment-item special-casing.

## Summary table citations (Key Blink files table, plan:673-684)

53. plan:675 — `core/html/resources/html.css:1701-1720` (dup of #1)
54. plan:676 — `core/css/resolver/style_adjuster.cc:247-356, 756-846` — blockify/inlinify.
55. plan:677 — `core/layout/layout_object.cc:430-435, 522-529` — `CreateObject`, `IsInlineRuby`, `IsInlineRubyText`.
56. plan:678 — `core/layout/layout_ruby_as_block.{h,cc}` — `LayoutRubyAsBlock` two-box model.
57. plan:679 — `core/layout/inline/inline_items_builder.cc:74, 1550-1700`.
58. plan:680 — `core/layout/inline/ruby_utils.{h,cc}` — full type list.
59. plan:681 — `core/layout/inline/line_breaker.cc:1059-1093, 1190, 2561-2608, 3278-3445`.
60. plan:682 — `core/layout/inline/inline_layout_algorithm.cc:381-418`.
61. plan:683 — `core/layout/inline/inline_node.cc:2103-2210`.
62. plan:684 — `core/style/computed_style_constants.h` — `EDisplay`, `ERubyAlign`, `ERubyOverhang`, `RubyPosition`.

## Unique file paths to verify

- `third_party/blink/renderer/core/html/resources/html.css`
- `third_party/blink/renderer/core/css/resolver/style_adjuster.cc`
- `third_party/blink/renderer/core/layout/layout_object.cc`
- `third_party/blink/renderer/core/layout/layout_ruby_as_block.{h,cc}`
- `third_party/blink/renderer/core/layout/inline/inline_items_builder.cc`
- `third_party/blink/renderer/core/layout/inline/ruby_utils.cc`
- `third_party/blink/renderer/core/layout/inline/ruby_utils.h`
- `third_party/blink/renderer/core/layout/inline/line_breaker.cc`
- `third_party/blink/renderer/core/layout/inline/inline_layout_algorithm.cc`
- `third_party/blink/renderer/core/layout/inline/inline_node.cc`
- `third_party/blink/renderer/core/style/computed_style_constants.h`
