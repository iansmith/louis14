# LOU css-ruby Phase 1 — reading checklist

Worktree: /Users/iansmith/louis14/.claude/worktrees/agent-aae89fc201fca4580
Branch: worktree-agent-aae89fc201fca4580
Pinned Blink SHA: 4883d11fef4a8713e32cd582ecef6dc5457c8c3f
Date: 2026-05-18

## Blink reads (confirmed via WebFetch at pinned SHA)

### html.css

Flat element-only display:none rule covering rp (lines 972-975):

    base, basefont, datalist, head, link, meta, noembed,
    noframes, param, rp, script, style, template, title {
      display: none;
    }

Ruby UA block (lines 1701-1720):

    /* HTML5 ruby elements */

    ruby, rt {
      text-indent: 0;
    }

    ruby {
      display: ruby;
    }

    rt {
      line-height: normal;
      text-emphasis: none;
    }

    ruby > rt {
      display: ruby-text;
      font-size: 50%;
      text-align: start;
    }

Confirmed: rb, rbc, rtc receive NO display override in UA. rt only becomes
display: ruby-text when the parent is a ruby (the unconditional rt rule
only sets line-height and text-emphasis). rp is hidden by the flat
element-only rule, not parent-scoped.

### style_adjuster.cc

EquivalentBlockDisplay:

    case EDisplay::kRuby:           return EDisplay::kBlockRuby;
    case EDisplay::kRubyText:       return EDisplay::kBlock;

EquivalentInlineDisplay:

    case EDisplay::kBlockRuby:      return EDisplay::kRuby;
    // kRubyText is already inline — unchanged.

AdjustStyleForDisplay inlinification (lines 788-805): when
layout_parent_style.InlinifiesChildren() and ShouldBeInlinified(element),
SetDisplay(EquivalentInlineDisplay(...)) is applied to every non-OOF
child and floats are dropped to float:none.

### layout_object.cc

CreateObject ruby cases (lines 430-435):

    case EDisplay::kRuby:       return LayoutInline(element);
    case EDisplay::kRubyText:   return LayoutInline(element);
    case EDisplay::kBlockRuby:  return LayoutRubyAsBlock(element);

IsInlineRuby/IsInlineRubyText (lines 522-529):

    IsInlineRuby() = IsLayoutInline() && Display() == kRuby
    IsInlineRubyText() = IsLayoutInline() && Display() == kRubyText

## Phase 1 plan summary

1. pkg/css/style.go: add DisplayBlockRuby; delete DisplayRubyBase + its
   parse case; add two-keyword "block ruby" / "inline ruby" parsing.
2. pkg/css/cascade.go: rewrite ruby UA block to mirror html.css exactly.
   rt → ruby-text ONLY when parent is ruby. rp → display:none flat. No
   display override for rb/rbc/rtc. font-size on rt = 50% (not 0.5em).
3. pkg/css/cascade.go: add NewAnonymousInlineRubyStyle helper.
4. pkg/layout/inline_layout.go: drop DisplayRubyBase from
   isInlineLevelDisplay.
5. pkg/layout/layout_tree_builder.go:
   - normalizeRubySubtrees: for display:block ruby elements, generate
     LayoutRubyAsBlock two-box structure (block principal + anonymous
     inline display:ruby holding the children).
   - inlinifyRubyChildren: when a node's layout parent is ruby or
     ruby-text, set in-flow children to their EquivalentInlineDisplay and
     force float:none. Recurse.
6. Update isBlockContainer / isBlockLevel for DisplayBlockRuby.

## Phase 1 callers of DisplayRubyBase (must be removed/replaced)

- pkg/css/style.go:4455      enum constant definition (delete)
- pkg/css/style.go:4531-4532 parse case (delete)
- pkg/css/cascade.go:181     `<rb>` UA sets "ruby-base" (delete; rb gets
  no UA display override)
- pkg/layout/inline_layout.go:70 isInlineLevelDisplay case (delete)
